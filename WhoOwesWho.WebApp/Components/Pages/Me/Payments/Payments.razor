@page "/me/payments"
@inherits AuthorizationComponentBase

@if (isLoading)
{
    <div class="flex justify-center items-center h-40">
        <div class="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-green-700"></div>
    </div>
}
else
{
    <div class="text-center px-4 mb-3">

        <div class="my-6 md:my-10">
            <h1 class="text-2xl md:text-4xl">
                Payments overview
            </h1>
        </div>

        @if (payments?.Payments == null)
        {
            <p class="text-base">
                No active payments available for this event!
            </p>
        }
        else
        {
            <PaymentsDataOverwiev PaymentsData="@payments"
                                  Active="@true" />

            @if (isAdministrator)
            {
                <EditForm method="post"
                          Model="SettleEventRequestModel"
                          OnSubmit="SettleEventAsync"
                          FormName="settlement"
                          Enhance>

                    <div class="flex justify-center mt-10">

                        <SubmitButton IsProcessing="@isProcessing"
                                      Text="Close event"
                                      ProcessingText="Processing..." />

                    </div>

                </EditForm>
            }
        }

    </div>
}