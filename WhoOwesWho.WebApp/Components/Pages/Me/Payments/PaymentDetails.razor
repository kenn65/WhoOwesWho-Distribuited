@page "/me/payment/details"
@inherits AuthorizationComponentBase

@if (isLoading)
{
    <div class="flex justify-center items-center h-40">
        <div class="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-green-700"></div>
    </div>
}
else
{
    <div class="text-center px-4 mb-3">

        <div class="my-6 md:my-10">

            <h1 class="text-2xl md:text-4xl">
                Payment Details
            </h1>

            <p class="text-lg md:text-2xl text-blue-800 mt-6 break-words">
                @eventResponseModel!.Name
                (@eventResponseModel.Location)
                starting at
                @eventResponseModel.StartDateIso
            </p>

            @if (isAdministrator)
            {
                <p class="text-lg md:text-2xl mt-6">
                    Edit or delete payment
                </p>
            }

        </div>

        @if (hasAssignment &&
             eventResponseModel != null &&
             currencies != null &&
             userBalanceResponseModel != null)
        {
            <EventPaymentElement HasAssignment="@hasAssignment"
                                 HasPayments="@hasPayments"
                                 IsProcessing="@isProcessing"
                                 EventResponseModel="@eventResponseModel"
                                 CurrencyList="@currencies"
                                 PaymentDetailsResponseModel="@paymentDetailsResponseModel"
                                 HandleUpdate="@OnUpdateAsync"
                                 HandleDelete="@OnDeleteAsync"
                                 Disabled="@(!isAdministrator || !active)"
                                 FromSettlement="!active" />
        }

    </div>
}